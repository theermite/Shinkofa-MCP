# Required — DEV.to API key (from https://dev.to/settings/extensions)
DEVTO_API_KEY=your-api-key-here

# Optional — Override API base URL (default: https://dev.to)
# DEVTO_BASE_URL=https://dev.to

# Optional — Request timeout in milliseconds (default: 30000)
# DEVTO_TIMEOUT_MS=30000
