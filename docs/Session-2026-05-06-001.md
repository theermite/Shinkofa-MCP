# Session 2026-05-06-001 — PM2 HTTP Deployment

## Work Done

- **ecosystem.config.cjs fix**: Spread all `envProd` variables into each PM2 server's env block so servers requiring API credentials can start
- **PM2 deployment**: Started all 23 MCP servers in HTTP mode
- **12 servers running**: stripe (9001), ffmpeg (9002), playwright (9003), system (9004), docker (9007), discord (9008), telegram (9009), obs (9011), twitch (9013), ollama (9015), imagemagick (9021), streamerbot (9023)
- **11 servers stopped** (missing credentials in .env.prod): obsidian, tailscale, gmail, youtube, n8n, google-calendar, google-drive, linkedin, devto, hashnode, home-assistant
- **PM2 state saved** for auto-restart on reboot
- **Verified MCP protocol**: initialize handshake on mcp-system returns 200 + session ID

## Tests

- mcp-shared: 35/35 passed (including transport.test.ts HTTP mode)

## Errors & Corrections

| Error | Cause | Fix |
|-------|-------|-----|
| 16/23 servers crash-looping | Missing API credentials (OBSIDIAN_API_KEY, GOOGLE_ACCESS_TOKEN, etc.) not in .env.prod | Stopped those servers; documented required vars |
| ecosystem.config only passed transport vars | Original config didn't spread envProd | Added `...envProd` spread in env block |

## Decisions

- Spread ALL envProd vars to all servers (simpler than per-server mapping; unused vars are harmless)
- Stopped servers without credentials rather than leaving them in crash-loop

## Pending Items

1. **Add missing credentials to .env.prod** — Jay needs to provide: OBSIDIAN_API_KEY, TAILSCALE_API_KEY, GOOGLE_ACCESS_TOKEN, YOUTUBE_API_KEY, N8N_API_KEY+N8N_BASE_URL, LINKEDIN_ACCESS_TOKEN, DEVTO_API_KEY, HASHNODE_PAT, HA_ACCESS_TOKEN+HA_BASE_URL
2. **Google OAuth flow** — Gmail/Calendar/Drive need GOOGLE_ACCESS_TOKEN (OAuth flow, not just client_id/secret)
3. **pm2 startup** — Run `pm2 startup` to generate systemd unit for boot persistence
4. **Commit + push remaining unstaged work** from previous session (large diff still in working tree)

## Context Awareness

- Session was short (continued from compaction). No threshold reached.

## Scoring V2

| Dimension | Score | Notes |
|-----------|-------|-------|
| Value | 75 | 12/23 servers deployed and verified; blocked by missing credentials (external dependency) |
| Reliability | 90 | 1 correction (envProd spread), zero regressions |
| Process | 70 | No reformulation needed (trivial single-file change); Obsidian sync skipped (MCP not invoked this short session) |
| **Total** | **78** | (75x0.4)+(90x0.3)+(70x0.3) = 30+27+21 = 78 |

## Visibility Check

Nothing shareable this session — deployment config is internal infrastructure.
