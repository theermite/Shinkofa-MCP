# Session 2026-04-26-001 — Shinkofa-MCP Phase 1+2+3 (Audit QE V2)

**Date** : 2026-04-26
**Durée** : ~longue (compaction intervenue mi-session)
**Branche** : main
**Commits** : 4 atomiques poussés sur origin/main
**Énergie Jay** : haute en début et milieu de session

---

## Mission

Corriger les 4 BLOCKING + 3 WARNING de l'audit QE V2 (`docs/audits/QE-V2-Pattern-Audit-2026-04-26.md`) **avant** de construire mcp-playwright. Décision Jay : "On travaille sur des bases saines."

---

## Travail accompli

### Phase 1 — Foundation (BLOCKING)

| Fix | Description |
|-----|-------------|
| **B1** | `biome.json` 2.4 à la racine, scripts `lint` migrés ESLint→Biome sur 22 serveurs |
| **B2** | `vitest.config.ts` (pool forks, maxForks 2, isolate) + `cross-env NODE_OPTIONS=--max-old-space-size=2048` partout |
| **B3** | `@shinkofa/mcp-shared@1.0.1` créé et publié sur Verdaccio (`npm.shinkofa.com`) — expose `toolResult`, `toolError`, `createErrorHandler` |
| **B4** | `.github/workflows/ci.yml` avec matrice 22 serveurs (tsc + biome + vitest + audit) + gitleaks + semgrep |

### Phase 2 — Propagation aux 22 serveurs

22/22 serveurs migrés vers `@shinkofa/mcp-shared`. Variantes traitées :
- Standard (15 serveurs) : import direct + formatter API-spécifique
- `mcp-ffmpeg` / `mcp-obs` / `mcp-streamerbot` : override local de `toolResult` pour passer les strings raw + catch-all `Error`
- `mcp-system` : conserve `SystemError`, `maskSecretValue()`, `isExecAllowed()` localement
- `mcp-discord` / `mcp-twitch` : double classe d'erreur (RateLimitError priorité)
- `mcp-gmail` : conserve `buildRawEmail()`, `sanitizeHeader()` localement
- `mcp-google-drive` : conserve `GOOGLE_WORKSPACE_TYPES`
- `mcp-imagemagick` : catch ALL Error (générique)

### Phase 3 — Hygiène complémentaire (WARNING)

| Fix | Description |
|-----|-------------|
| **W1** | TLS bypass scopé dans mcp-obsidian : `process.env["NODE_TLS_REJECT_UNAUTHORIZED"] = "0"` (process-wide) → `undici.Agent({ connect: { rejectUnauthorized: false }})` scopé au client. Tests mis à jour pour mocker `undici` au lieu du `fetch` global |
| **W3** | `@vitest/coverage-v8` ajouté aux 8 serveurs manquants + script `test:coverage` ajouté aux 5 qui n'en avaient pas. 22/22 couverts |
| **W2** | 10 propriétés fast-check ajoutées à `@shinkofa/mcp-shared` (PBT — anti-circular Layer 1). Tests : roundtrip JSON, preservation Unicode, formatter priority chain, fall-through |

### Décision validée — Verdaccio over npm workspace (B3)

Analyse Devil's Advocate sous lentille QE V2. Verdaccio retenu pour :
- Lego First consistency (convention `@shinkofa/*` déjà établie)
- Indépendance des serveurs (chacun standalone, pas de couplage workspace)
- Infrastructure déjà en place (`npm.shinkofa.com`)

---

## Tests

| Scope | Résultat |
|-------|----------|
| `@shinkofa/mcp-shared` | **28/28** (18 example + 10 PBT) |
| `mcp-obsidian` (TLS scope) | **95/95** |
| `mcp-telegram` (smoke) | **109/109** |
| `mcp-discord` (smoke) | **235/235** |
| Total vérifié | **467/467** |
| Phase 2 propagation (avant compaction) | **2 048/2 048** |

Aucune régression. Pre-existing `BodyInit TS2304` dans mcp-telegram noté, non lié à la migration.

---

## Erreurs rencontrées + corrections

| Erreur | Cause | Fix |
|--------|-------|-----|
| Biome 2.0 vs 2.4 config format | Schema URL et `files.ignore` obsolètes | Réécriture avec `$schema: 2.4`, `files.includes`, `javascript.assist` |
| `ToolResponse` rejeté par MCP SDK | `CallToolResult` exige `[key: string]: unknown` | Ajout index signature, republish 1.0.1 |
| E404 sur `@shinkofa/mcp-shared` | `.npmrc` racine non hérité par sub-projects | `npm config set @shinkofa:registry` au niveau user |
| 4 serveurs : test SyntaxError mismatch | Message API-spécifique vs générique partagé | Tests mis à jour vers message générique |
| 3 serveurs : catch-all Error perdu | Migration ne capturait que API class | Ajout `if (error instanceof Error)` dans formatters |
| 3 serveurs : `toolResult` raw strings | Original passait strings raw, shared JSON.stringify | Override local de `toolResult` (delegate objects to shared) |
| Tests obsidian : self-signed cert | `vi.stubGlobal("fetch")` ne mocke pas `undici.fetch` | `vi.mock("undici", ...)` avec `vi.hoisted` |
| `fc.fullUnicodeString` undefined | fast-check v4 a retiré l'API | `fc.string()` (déjà unicode en v4) |
| VirtualAlloc OOM transient | Pression mémoire après 8 npm installs parallèles | Retry — pas un vrai bug |

---

## Décisions

1. **Verdaccio retenu** comme registry pour les packages partagés `@shinkofa/*` (Devil's Advocate validé sous lentille QE V2)
2. **`createErrorHandler` factory pattern** : chaque serveur fournit son `ErrorFormatter`, fallback automatique vers les handlers built-in (Abort/Syntax/TypeError)
3. **PBT placée dans `@shinkofa/mcp-shared`** plutôt que par serveur — leverage maximum (toutes les 22 servers en bénéficient indirectement)
4. **Session fraîche pour mcp-playwright** : couper après Phase 3 pour repartir sur un contexte propre

---

## Pending

- **mcp-playwright** : nouveau serveur MCP à construire sur fondations corrigées (Phase 4 de l'audit)
- **StrykerJS** : mutation testing mensuelle CI — non implémenté (audit suggérait "monthly")
- **fast-check sur tools critiques** : le PBT a été ajouté à mcp-shared (leverage max), mais des PBT par-serveur sur les tools critiques (obsidian write, telegram send, discord send, stripe payment) restent à faire ultérieurement
- **Pre-existing `BodyInit TS2304`** dans mcp-telegram à investiguer
- **CI run** : workflow créé mais pas encore vérifié sur un push (à observer au prochain push)

---

## Context Awareness

**Compaction intervenue** mi-session après Phase 2. Reprise propre via summary — aucune perte de contexte fonctionnelle. Phase 3 démarrée avec contexte frais.

Pas de signaux de dégradation (pas de circular failures, pas de regressions répétées). Recommandation Takumi de session fraîche pour mcp-playwright accepted by Jay.

---

## Visibility Check

**Candidats partageables** :

1. **Pattern `createErrorHandler` factory** — article potentiel : "Building a generic error handler factory for MCP servers in TypeScript" (Dev.to / Hashnode). Taille ~300-500 lignes, pédagogique, problème courant.
2. **Anti-circular PBT pattern** — article potentiel : "Property-based testing as anti-circular validation for AI-generated code" (lien direct avec QE V2 methodology, pourrait toucher la communauté testing).
3. **Mini-repo `@shinkofa/mcp-shared`** — déjà sur Verdaccio privé, pourrait devenir un mini-package open-source si Jay le souhaite (pattern réutilisable pour tout MCP server builder).

À ajouter dans Obsidian `01-Projets/Contenu.md` sous "Idées / candidates".

---

## Scoring V2

| Dimension | Score | Notes |
|-----------|-------|-------|
| Value | 90 | 4 BLOCKING + 3 WARNING résolus, 22 serveurs migrés, fondations saines pour Phase 4. Concret, mesurable, push prod (origin/main) |
| Reliability | 85 | Quelques re-runs dûs aux écarts entre Biome v2.0/2.4, undici imports, fast-check v4 API. Aucune régression escapée, toutes corrections in-session |
| Process | 95 | Tous les gates respectés (TDG, atomic commits, reformulation avant Phase 2 et Phase 3, Obsidian sync à venir). Manqué : pas de Blueprint update formel (pas requis pour fix d'audit) |
| **Total** | **(90×0.4)+(85×0.3)+(95×0.3) = 36+25.5+28.5 = 90** | |

Human Quality Gates : N/A — session sur infrastructure, pas de feature publique.

---

## Commits poussés

| SHA | Message |
|-----|---------|
| `56715c4` | feat(mcp-shared): create shared package with toolResult, toolError, createErrorHandler |
| `2f6e62c` | refactor(servers): migrate 22 servers to @shinkofa/mcp-shared |
| `d4c3003` | fix(mcp-obsidian): scope TLS bypass to undici Agent instead of process-wide env |
| `787e168` | chore(quality): add coverage-v8 to 7 servers + PBT to mcp-shared |

---

**Prochaine session** : `/session-start` puis attaque mcp-playwright (Phase 4 de l'audit) sur fondations propres.
